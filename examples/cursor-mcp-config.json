{
  "mcpServers": {
    "devops": {
      "command": "mcp-server",
      "args": ["--transport", "stdio"],
      "env": {},
      "description": "MCP DevOps Server with Task Master - SSH, Docker, Kubernetes, and task management"
    }
  }
}
